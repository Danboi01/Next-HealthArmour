Config = {

    NotifyEnabled = true, -- Notifications | true = on | false = off

    Prefix = "Server Name", -- Prefix for Notification (Title)

    NotifyDuration = 8000,  -- Duration for notifications (only relevant for certain notification systems like okokNotify or codem)

    Notify = 3,  -- Notification system to use: 0 = chat message, 1 = okokNotify, 2 = codem, 3 = ox_lib

}
